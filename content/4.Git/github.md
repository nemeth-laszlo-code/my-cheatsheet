# GH-pages nuxt projekt-hez
```bash
npm i gh-pages
```
- a scriptekhez

```json
    "deploy": "nuxi generate && gh-pages -d .output/public"
```

---

- így npm run deploy és már fennt is van a statikus nuxt oldalunk